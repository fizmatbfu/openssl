#ifndef  __OPENSSL_COMMONCONSTANTS__
#define  __OPENSSL_COMMONCONSTANTS__

const size_t kNumOfMegabytesForTest = 20;

constexpr size_t kSize = kNumOfMegabytesForTest * 1024 * 1024 * 8 / sizeof(unsigned char);

#endif // ! __OPENSSL_COMMONCONSTANTS__

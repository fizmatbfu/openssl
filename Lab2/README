Elmir Kurakin Lab 2.

Code requires: libstdc++, libcrypto, libssl

For build and run lab, run following commands:
make
./cipherbench

Note: may have problems caused side-realization of GOST15 (Grasshopper) (missing includes and libs).
I used this path for OpenSSL: https://github.com/ddulesov/engine